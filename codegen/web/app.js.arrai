\(:model, :appName, config: (:host, :port), ...)
    $`
// ${appName::}

window.gen = window.gen || {};
window.gen.app = {
    name: '${appName::}',
    userId: 'user' + Math.floor(Math.random() * 1000000),
};
`
